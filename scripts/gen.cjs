const fs = require('fs');
const path = require('path');
const target = path.join(__dirname, '..', 'src', 'components', 'admin', 'SettingsForm.tsx');
const parts = [];
function w(s) { parts.push(s); }
